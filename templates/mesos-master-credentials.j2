{
  "credentials": [
    {
      "principal": "mesos-slave",
      "secret": "{{ mesos_slave_secret }}"
    },
    {
      "principal": "marathon",
      "secret": "{{ marathon_secret }}"
    },
    {
      "principal": "es",
      "secret": "{{ mesos_framework_password }}"
    }
  ]
}

