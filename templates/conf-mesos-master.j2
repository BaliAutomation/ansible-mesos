# {{ansible_managed}}

export MESOS_QUORUM="{{mesos_quorum}}"
export MESOS_WORK_DIR="{{mesos_work_dir}}"
export MESOS_PORT="{{mesos_master_port}}"

export MESOS_CREDENTIALS="/etc/mesos/master/credentials"

export MESOS_AUTHENTICATE="{{mesos_authenticate_all}}"
export MESOS_AUTHENTICATE_AGENTS="{{mesos_authenticate_all}}"
export MESOS_AUTHENTICATE_HTTP="{{mesos_authenticate_all}}"

